# Agent Context

- The canonical agent instructions are in `AGENTS.md`.
- The current target is `projects/my-life-story/`.
- This repository uses `uv`; run Python through `uv run`.
- Manga production must use Comfy for generation and Krita for lettering/finishing.
- `.\manga.ps1 validate -Project my-life-story` must pass before a manga is called complete.
