[project]
name = "manga-system"
version = "0.1.0"
description = "Local ComfyUI-to-Krita manga production harness"
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.23,<5",
    "pillow>=11,<13",
    "pyyaml>=6,<7",
]

[tool.uv]
package = false

